# Config local de herramientas (preview de Claude)
.claude/

# Carpeta fuente de los favicons (los definitivos viven en la raíz: favicon.ico, favicon.svg, apple-touch-icon.png)
Favicon/

# Imágenes fuente sin optimizar (las versiones finales van sin sufijo)
assets/img/*-original.*

# Archivos del sistema
Thumbs.db
.DS_Store

# Dependencias (por si en el futuro se agrega tooling)
node_modules/

# Workflows de n8n exportados (artefactos para importar; contienen IDs internos)
*.n8n.json
